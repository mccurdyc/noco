# nocodb

My personal [NocoDB](https://www.nocodb.com) setup.

```zsh
# fetch the dump
make fetch
```

```zsh
# run the container
make run
```

```zsh
# sync the dump
make dump
```

Visit `http://nuc:8080` in the browser.
